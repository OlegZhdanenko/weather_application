// import { selectNameFilter } from "../filters/selectors.js";
// import { createSelector} from "@reduxjs/toolkit";

// export const selectWeatherTemp = (state) => state.weather.temp;
// export const selectWeatherCurrent = (state) => state.weather.current;
// export const selectFilteredContacts = createSelector([selectContacts, selectNameFilter],
//         (contacts, filterName) => {
//                 return contacts.filter((contact) =>
//                         contact.name.toLowerCase().includes(filterName.toLowerCase())
//                 )
//         });
